// Define a simple model (optional but recommended)
class Tag {
  final String id;
  final String label;
  final String counterpart;

  const Tag({
    required this.id,
    required this.label,
    required this.counterpart,
  });
}

